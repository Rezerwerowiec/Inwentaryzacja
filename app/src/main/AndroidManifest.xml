<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pfhb.damian.inwentaryzacja">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inwentaryzacja">
        <activity
            android:name=".AddItemActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogsDisplayActivity"
            android:exported="true" />
        <activity
            android:name=".ReportGraphActivity"
            android:exported="true" />
        <activity
            android:name=".BarcodeInfoActivity"
            android:exported="true" />
        <activity
            android:name=".PutProductActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>